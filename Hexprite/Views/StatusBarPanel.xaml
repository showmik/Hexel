<UserControl x:Class="Hexprite.Views.StatusBarPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="1000">
    <Border>
        <Border.Style>
            <Style TargetType="Border" BasedOn="{StaticResource StatusBarPanel}">
                <Setter Property="Visibility" Value="Visible" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding}" Value="{x:Null}">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Tool Name -->
            <TextBlock Grid.Column="0"
                       Text="{Binding CurrentTool, StringFormat={}Tool: {0}}"
                       Foreground="{DynamicResource Brush.Text.Muted}"
                       VerticalAlignment="Center"
                       Margin="0,0,15,0" />

            <!-- Coordinates -->
            <TextBlock Grid.Column="1"
                       Foreground="{DynamicResource Brush.Text.Muted}"
                       VerticalAlignment="Center"
                       Margin="0,0,15,0">
                <Run Text="X: " /><Run Text="{Binding CursorX}" /><Run Text=", Y: " /><Run Text="{Binding CursorY}" />
            </TextBlock>

            <!-- Canvas Dimensions -->
            <TextBlock Grid.Column="2"
                       Text="{Binding CanvasDimensionText}"
                       Foreground="{DynamicResource Brush.Text.Muted}"
                       VerticalAlignment="Center"
                       Margin="0,0,15,0" />

            <!-- Zoom Level -->
            <TextBlock Grid.Column="3"
                       Text="{Binding ZoomLevel, StringFormat={}Zoom: {0:F1}x}"
                       Foreground="{DynamicResource Brush.Text.Muted}"
                       VerticalAlignment="Center"
                       Margin="0,0,15,0" />

            <!-- Selection Info (shown when selection exists) -->
            <TextBlock Grid.Column="4"
                       Text="{Binding SelectionInfo}"
                       Foreground="{DynamicResource Brush.Accent.Base}"
                       VerticalAlignment="Center"
                       Margin="0,0,15,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectionInfo}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Layer Hover Info (shown when hovering layers) -->
            <TextBlock Grid.Column="5"
                       Text="{Binding LayerHoverInfo}"
                       Foreground="{DynamicResource Brush.Text.Muted}"
                       VerticalAlignment="Center"
                       Margin="0,0,15,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding LayerHoverInfo}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Status message (center-right, data-bound) -->
            <TextBlock Grid.Column="6"
                       Text="{Binding StatusMessage}"
                       Foreground="{DynamicResource Brush.Accent.Base}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Padding="0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusMessage}" Value="">
                                <Setter Property="Visibility" Value="Hidden" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Keyboard Modifiers (rightmost) -->
            <StackPanel Grid.Column="7"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Margin="15,0,0,0">
                <TextBlock Text="Shift"
                           Foreground="{DynamicResource Brush.Accent.Base}"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="0,0,6,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsShiftPressed}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Text="Ctrl"
                           Foreground="{DynamicResource Brush.Accent.Base}"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="0,0,6,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCtrlPressed}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Text="Alt"
                           Foreground="{DynamicResource Brush.Accent.Base}"
                           FontWeight="Bold"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAltPressed}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
